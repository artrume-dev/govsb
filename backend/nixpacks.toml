[phases.setup]
nixPkgs = ["python310", "gcc", "rustc", "cargo"]

[phases.install]
cmds = [
  "pip install --upgrade pip setuptools wheel",
  "cd backend && pip install -r requirements.txt"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
