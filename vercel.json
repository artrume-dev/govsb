{
  "buildCommand": "ls -la frontend/dist/index.html && echo 'Pre-built files ready'",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "installCommand": "echo 'No installation needed'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
